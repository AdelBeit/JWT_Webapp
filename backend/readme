plan:


-- routes
> login call
> > check credentials
> > > get JWT
> > > redirect to page with token
> rest of page calls
> > check JWT
> logout call

-- models
mongo
> authenticate user and password
> authenticate user and token
> > save token with user
JWT
> generate token
